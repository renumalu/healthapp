# Supabase Configuration
# These are your PUBLIC Supabase project credentials
# The Publishable Key is safe to expose in the frontend
# Never commit your actual .env file with real values to git!

VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key-here"

# SECURITY NOTES:
# - These variables are exposed in the browser (they are marked as VITE_ prefix)
# - Do NOT put any secret keys here (like JWT secrets or API keys)
# - User secrets and private API keys should only be on the backend/Edge Functions
# - All sensitive operations should go through Supabase Auth with access tokens
